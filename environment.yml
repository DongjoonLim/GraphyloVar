name: graphylovar
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.8
  - numpy>=1.20.3,<2.0.0
  - pandas>=1.3.4,<2.0.0
  # SECURITY: TensorFlow upgraded from 2.5.0 to 2.12.1+ due to critical CVEs
  # See SECURITY.md for vulnerability details and migration guide
  - tensorflow>=2.12.1,<2.13.0
  - spektral
  - scikit-learn
  - biopython
  - tqdm
  - pybigwig
  - networkx
  - matplotlib
  - pip
  - pip:
      - focal_loss
      # Development tools (optional - uncomment to install)
      # - pytest>=6.2.0
      # - flake8>=4.0.0
      # - black>=21.0
      # - mypy>=0.910
      # - isort>=5.9.0

