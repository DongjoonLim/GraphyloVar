name: graphylovar
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.8
  - numpy=1.20.3
  - pandas=1.3.4
  - tensorflow=2.5.0
  - spektral
  - scikit-learn
  - biopython
  - tqdm
  - pybigwig
  - networkx
  - matplotlib
  - pip
  - pip:
      - focal_loss
      # Development tools (optional - uncomment to install)
      # - pytest>=6.2.0
      # - flake8>=4.0.0
      # - black>=21.0
      # - mypy>=0.910
      # - isort>=5.9.0

